import {NextResponse} from "next/server"; export async function POST(req){try{const data=await req.formData();const title=data.get("title"),price=data.get("price"),link=data.get("link"),image=data.get("image");const token=process.env.TELEGRAM_BOT_TOKEN;const chat=process.env.TELEGRAM_CHANNEL||"@CacaOfertaBR_Oficial";if(!token)return NextResponse.json({error:"TELEGRAM_BOT_TOKEN não configurado"},{status:500});const caption=`🔥 <b>OFERTA ENCONTRADA!</b>\n\n🛍️ <b>${title}</b>\n💰 <b>${price}</b>\n\n🛒 <a href="${link}">PEGAR OFERTA</a>\n\n⚡ Preço pode mudar a qualquer momento.\n🔎 <b>Caça Oferta</b> — A gente caça. Você economiza.`;const method=image?"sendPhoto":"sendMessage";const body=image?{chat_id:chat,photo:image,caption,parse_mode:"HTML"}:{chat_id:chat,text:caption,parse_mode:"HTML",disable_web_page_preview:false};const r=await fetch(`https://api.telegram.org/bot${token}/${method}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const out=await r.json();if(!out.ok)return NextResponse.json(out,{status:400});return NextResponse.redirect(new URL("/",req.url),303)}catch(e){return NextResponse.json({error:e.message},{status:500})}}